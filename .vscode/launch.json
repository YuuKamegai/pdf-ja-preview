{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run PDF",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/dist-web/server.cjs",
      "args": ["--open"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
